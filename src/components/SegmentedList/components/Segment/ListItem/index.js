import React from 'react'
import PropTypes from 'prop-types';

export const ListItem = ({ deletable, editable }) => {

};

ListItem.propTypes = {
	deletable: PropTypes.bool,
	editable: PropTypes.bool,
};
