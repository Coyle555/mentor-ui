import React from 'react';
import PropTypes from 'prop-types';

const NoResultsFound = (props) => (
	<li className="apm-list-filter-menu-li">
		<i>No results found.</i>
	</li>
);

export default NoResultsFound;
